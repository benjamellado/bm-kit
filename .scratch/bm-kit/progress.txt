## Codebase Patterns
- `context/workflow.md` is the single shared context file loaded by every skill — update it when the pipeline changes, not each skill individually
- Issue frontmatter fields: `id`, `title`, `status`, `priority` (YAML, not bare key-value)
- Valid status values: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`, `done`
- `docs/agents/issue-tracker.md` is the authoritative spec for `.scratch/` format — all skills reference it

---

## 2026-05-03 - BM-02
- Updated `docs/agents/issue-tracker.md` with full `.scratch/` format spec: directory layout, frontmatter fields, valid status values, filename convention `NN-slug.md`, PRD frontmatter
- Created `context/workflow.md` at plugin root covering the full pipeline (install → init → grill-me → grill-with-docs → to-prd → to-issues → triage → ralph), purpose of each skill, `.scratch/` structure, status lifecycle diagram
- Files changed: `docs/agents/issue-tracker.md`, `context/workflow.md`
- **Learnings for future iterations:**
  - `docs/agents/issue-tracker.md` did not exist before (created from scratch despite appearing to be an edit)
  - `context/` directory did not exist — created it
  - The workflow.md file is the contract every skill SKILL.md must load via `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`
---

## 2026-05-03 - BM-03
- Created `skills/init/SKILL.md` — new skill that bootstraps a project for bm-kit workflow
- Skill loads `${CLAUDE_PLUGIN_ROOT}/context/workflow.md` before acting
- Creates `.scratch/`, `CONTEXT.md` stub, `docs/adr/`, and `progress.txt` stub if they don't exist
- Ends with "Project initialised. Run `/bm-kit:grill-me` to start your first feature."
- Files changed: `skills/init/SKILL.md`, `.scratch/bm-kit/issues/03-init-skill.md`
- **Learnings for future iterations:**
  - `allowed-tools: Bash, Write` in frontmatter — init needs both to create dirs and write stubs
  - CONTEXT.md and progress.txt stubs are included inline in the skill prompt so Claude writes consistent content without guessing
---

## 2026-05-03 - BM-04
- Rewrote `scripts/ralph.sh`: accepts `<feature-name>` as first arg, optional `--tool claude` (default), reads `ready-for-agent` issues from `.scratch/<feature>/issues/` in filename order, prepends feature context to CLAUDE.md before piping to claude, exits cleanly when no ready-for-agent issues remain
- Rewrote `CLAUDE.md`: removed all `prd.json` references, updated task steps to read from the passed issue file, update `status: done` in frontmatter, append to both feature and global progress logs
- Files changed: `scripts/ralph.sh`, `CLAUDE.md`
- **Learnings for future iterations:**
  - Script prepends a `## Current Task` block (feature name, issue file path, progress log paths) before CLAUDE.md content — this is how the agent knows which issue to work on without the script having to parse or transform the issue
  - `while IFS= read -r f; do ... done < <(find ... | sort)` is the correct bash pattern for sorted iteration — avoids subshell variable scoping issues
  - `prd.json`, archive logic, and `.last-branch` file are all removed — no backwards compatibility needed
---

## 2026-05-03 - BM-06
- Rewrote `skills/to-prd/SKILL.md`: removed GitHub API calls, added `${CLAUDE_PLUGIN_ROOT}/context/workflow.md` load, writes PRD to `.scratch/<feature-slug>/PRD.md` with `status: needs-triage` frontmatter, asks for slug if not clear, ends with `/bm-kit:to-issues` suggestion
- Files changed: `skills/to-prd/SKILL.md`, `.scratch/bm-kit/issues/06-to-prd-skill-rewrite.md`
- **Learnings for future iterations:**
  - `allowed-tools: Bash, Read, Write` — skill needs Write to create the PRD file and Bash for `mkdir -p`
  - Include `mkdir -p` instruction so Claude creates the directory before writing
---

## 2026-05-03 - BM-07
- Rewrote `skills/to-issues/SKILL.md`: vertical slice approach preserved, issues now written to `.scratch/<feature-slug>/issues/NN-slug.md` with full frontmatter, no GitHub API calls, ends with `/bm-kit:triage` suggestion
- Files changed: `skills/to-issues/SKILL.md`, `.scratch/bm-kit/issues/07-to-issues-skill-rewrite.md`
- **Learnings for future iterations:**
  - Include explicit `## Comments` section in the issue template with AI disclaimer — triage skill expects it
  - Feature ID prefix (e.g. `BM`) should be determined from context or asked — document this clearly
---

## 2026-05-03 - BM-08
- Rewrote `skills/triage/SKILL.md`: GitHub label calls replaced with frontmatter `status:` edits via Edit tool, appends `## Comments` entries with triage reasoning, checks all issues after triaging and suggests `/bm-kit:ralph` when all are ready
- Files changed: `skills/triage/SKILL.md`, `.scratch/bm-kit/issues/08-triage-skill-rewrite.md`
- **Learnings for future iterations:**
  - `allowed-tools: Bash, Read, Edit` — triage modifies existing files (Edit), reads them (Read), and may list directories (Bash)
  - No `.out-of-scope/` directory in bm-kit — removed references to it; wontfix just appends a comment
---

## 2026-05-03 - BM-09
- Added `${CLAUDE_PLUGIN_ROOT}/context/workflow.md` context load at top of 7 skills: grill-me, grill-with-docs, improve-codebase-architecture, diagnose, tdd, write-a-skill, caveman
- Added next-step suggestions at end of each (except caveman which is a mode toggle)
- Files changed: all 7 skill SKILL.md files, `.scratch/bm-kit/issues/09-planning-utility-skills-polish.md`
- **Learnings for future iterations:**
  - Additive edits only — core prompting logic untouched; just prepend context load and append next-step
  - caveman gets the context load but no next-step (it's a toggle, not a workflow step)
---

## 2026-05-03 - BM-10
- Replaced old Ralph README entirely with bm-kit README: 2-3 sentence intro, one-command install, per-project init, workflow table, `.scratch/` structure, status lifecycle, credits section
- Files changed: `README.md`, `.scratch/bm-kit/issues/10-readme.md`
- **Learnings for future iterations:**
  - Keep README focused on the workflow pipeline table — that's what users need to remember after time away
  - Credits section is important for transparency about origins
---

## 2026-05-03 - BM-05
- Created `skills/ralph/SKILL.md` — user-facing entry point to the ralph implementation loop
- Skill loads `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`, shows queued `ready-for-agent` issues before running, invokes `bash ${CLAUDE_PLUGIN_ROOT}/scripts/ralph.sh <feature-slug>`, ends with pointer to feature progress.txt
- Files changed: `skills/ralph/SKILL.md`, `.scratch/bm-kit/issues/05-ralph-skill.md`
- **Learnings for future iterations:**
  - `allowed-tools: Bash` only — skill doesn't write files itself, it delegates to ralph.sh
  - The "show queued issues" step before running is important UX — user should know what's about to run
  - Pattern: skill body describes what to do in plain language; the actual bash command is embedded inline
---
