---
id: BM-09
title: Planning & utility skills polish
status: ready-for-agent
category: enhancement
priority: 9
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

Update all remaining skills — `grill-me`, `grill-with-docs`, `improve-codebase-architecture`, `diagnose`, `tdd`, `write-a-skill`, `caveman` — to load the shared workflow context and suggest the next step. Core logic of each skill is preserved.

## Acceptance criteria

- [ ] All seven skills copied from `.agents/skills/` into `skills/` and published in the plugin
- [ ] Each skill loads `${CLAUDE_PLUGIN_ROOT}/context/workflow.md` at the top of its SKILL.md
- [ ] `grill-me` ends by suggesting `/bm-kit:grill-with-docs`
- [ ] `grill-with-docs` ends by suggesting `/bm-kit:to-prd`
- [ ] `improve-codebase-architecture`, `diagnose`, `tdd` end with a relevant next-step suggestion
- [ ] `write-a-skill` and `caveman` end with a relevant next-step suggestion
- [ ] No changes to core prompting logic of any skill

## Blocked by

- BM-02

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

Additive changes only — no core logic modifications. Seven skills, each gets two additions: context load at top, next-step suggestion at bottom.

**Agent brief:**
- Read `context/workflow.md` for context
- For each skill in `skills/`: `grill-me`, `grill-with-docs`, `improve-codebase-architecture`, `diagnose`, `tdd`, `write-a-skill`, `caveman`
  - Add at top of SKILL.md body: `Read ${CLAUDE_PLUGIN_ROOT}/context/workflow.md for workflow context.`
  - Add next-step suggestion at end:
    - `grill-me` → "Ready to lock down language? Run `/bm-kit:grill-with-docs`."
    - `grill-with-docs` → "Ready to write the PRD? Run `/bm-kit:to-prd`."
    - `improve-codebase-architecture` → "Found issues to fix? Run `/bm-kit:grill-me` to plan the refactor."
    - `diagnose` → "Root cause found? Run `/bm-kit:tdd` to write a regression test."
    - `tdd` → "Tests green? Run `/bm-kit:grill-me` to plan the next feature."
    - `write-a-skill` → "Skill written? Run `/bm-kit:init` in a test project to verify it loads."
    - `caveman` → (no next-step needed — mode toggle)
  - Do NOT change any other content in the skills
- Commit with `feat: BM-09 - Planning & utility skills polish`
