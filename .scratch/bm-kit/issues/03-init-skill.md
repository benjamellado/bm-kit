---
id: BM-03
title: Init skill
status: done
category: enhancement
priority: 3
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

A new `/bm-kit:init` skill that sets up any project for the bm-kit workflow. Running it in a project creates the required directory structure and stub files, then tells the user what to do next.

## Acceptance criteria

- [ ] `skills/init/SKILL.md` created and published in the plugin
- [ ] Skill loads shared workflow context from `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`
- [ ] Skill creates `.scratch/` directory in the current project if it doesn't exist
- [ ] Skill creates `CONTEXT.md` stub in the project root if it doesn't exist
- [ ] Skill creates `docs/adr/` directory if it doesn't exist
- [ ] Skill creates global `progress.txt` at project root if it doesn't exist
- [ ] Skill ends by telling the user to run `/bm-kit:grill-me` to start a feature

## Blocked by

- BM-01
- BM-02

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

New skill with clear scope. Depends on BM-01 (skills/ directory) and BM-02 (workflow context file).

**Agent brief:**
- Read `.scratch/bm-kit/PRD.md` and `context/workflow.md` for context
- Create `skills/init/SKILL.md`
- Skill frontmatter: `allowed-tools: Bash, Write`, description explaining it sets up a project for bm-kit
- Skill body: load `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`, then create `.scratch/`, `CONTEXT.md` stub, `docs/adr/`, `progress.txt` if they don't exist
- End with: "Project initialised. Run `/bm-kit:grill-me` to start your first feature."
- Commit with `feat: BM-03 - Init skill`
