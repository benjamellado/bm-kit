---
id: BM-05
title: Ralph skill
status: ready-for-agent
category: enhancement
priority: 5
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

A new `/bm-kit:ralph` skill that invokes the ralph loop script from the plugin installation directory. The skill is the user-facing entry point to the implementation loop.

## Acceptance criteria

- [ ] `skills/ralph/SKILL.md` created and published in the plugin
- [ ] Skill loads shared workflow context from `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`
- [ ] Skill accepts a feature name argument and passes it to `${CLAUDE_PLUGIN_ROOT}/scripts/ralph.sh`
- [ ] Skill explains what `ready-for-agent` issues it will pick up before starting
- [ ] Skill ends by telling the user to check `.scratch/<feature>/progress.txt` for results

## Blocked by

- BM-01
- BM-04

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

Thin wrapper skill. Most complexity lives in the script (BM-04).

**Agent brief:**
- Read `context/workflow.md` and `scripts/ralph.sh` for context
- Create `skills/ralph/SKILL.md`
- Skill frontmatter: `allowed-tools: Bash`, description explaining it runs the ralph implementation loop
- Skill body: load `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`, accept feature name, run `bash ${CLAUDE_PLUGIN_ROOT}/scripts/ralph.sh <feature-name>`, explain what issues it will pick up
- End with: "Check `.scratch/<feature>/progress.txt` for learnings from this run."
- Commit with `feat: BM-05 - Ralph skill`
