---
id: BM-06
title: to-prd skill rewrite
status: ready-for-agent
category: enhancement
priority: 6
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

Rewrite the `to-prd` skill to write PRDs to `.scratch/feature/PRD.md` instead of GitHub issues. The skill synthesises the current conversation into a structured PRD document and saves it locally with `needs-triage` status.

## Acceptance criteria

- [ ] `skills/to-prd/SKILL.md` rewritten and published in the plugin
- [ ] Skill loads shared workflow context from `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`
- [ ] Skill writes the PRD to `.scratch/<feature-slug>/PRD.md` with `status: needs-triage` frontmatter
- [ ] Skill prompts user for a feature slug if not inferable from context
- [ ] PRD follows the template from the current skill (problem statement, solution, user stories, implementation decisions, testing decisions, out of scope, further notes)
- [ ] No GitHub API calls — fully local
- [ ] Skill ends by suggesting `/bm-kit:to-issues` as the next step

## Blocked by

- BM-02

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

Rewrite with local output. Remove all GitHub API calls, replace with Write tool to `.scratch/`.

**Agent brief:**
- Read `context/workflow.md` and existing `.agents/skills/to-prd/SKILL.md` for context
- Rewrite `skills/to-prd/SKILL.md`:
  - Load `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`
  - Synthesise conversation into PRD using the existing template (problem statement, solution, user stories, implementation decisions, testing decisions, out of scope, further notes)
  - Write to `.scratch/<feature-slug>/PRD.md` with `status: needs-triage` frontmatter
  - If feature slug not clear from context, ask user for it
  - No GitHub API calls
  - End: "PRD saved. Run `/bm-kit:to-issues` to break it into issues."
- Commit with `feat: BM-06 - to-prd skill rewrite`
