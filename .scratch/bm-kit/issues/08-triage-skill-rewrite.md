---
id: BM-08
title: Triage skill rewrite
status: ready-for-agent
category: enhancement
priority: 8
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

Rewrite the `triage` skill to operate on `.scratch/` markdown issue files instead of GitHub labels. The skill reads the issue frontmatter, applies the triage state machine, and writes the updated `status` back to the file.

## Acceptance criteria

- [ ] `skills/triage/SKILL.md` rewritten and published in the plugin
- [ ] Skill loads shared workflow context from `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`
- [ ] Skill reads `status` from issue frontmatter to determine current triage state
- [ ] Skill applies the state machine: `needs-triage` → `needs-info` | `ready-for-agent` | `ready-for-human` | `wontfix`
- [ ] Skill writes updated `status` back to the issue frontmatter
- [ ] Skill adds a `## Comments` entry explaining the triage decision
- [ ] No GitHub API calls — fully local
- [ ] When all feature issues are `ready-for-agent`, skill suggests `/bm-kit:ralph <feature>` as next step

## Blocked by

- BM-02

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

Rewrite for local file operations. State machine logic is preserved, GitHub label calls replaced with frontmatter edits.

**Agent brief:**
- Read `context/workflow.md`, `docs/agents/issue-tracker.md`, and existing `.agents/skills/triage/SKILL.md` for context
- Rewrite `skills/triage/SKILL.md`:
  - Load `${CLAUDE_PLUGIN_ROOT}/context/workflow.md`
  - Read `status` from issue frontmatter
  - Apply state machine: `needs-triage` → `needs-info` | `ready-for-agent` | `ready-for-human` | `wontfix`
  - Write updated `status` back to frontmatter using Edit tool
  - Append `## Comments` entry with triage reasoning
  - No GitHub API calls
  - When all feature issues are `ready-for-agent`: "All issues ready. Run `/bm-kit:ralph <feature>` to start implementation."
- Commit with `feat: BM-08 - Triage skill rewrite`
