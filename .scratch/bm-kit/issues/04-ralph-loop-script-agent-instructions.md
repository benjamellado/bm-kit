---
id: BM-04
title: Ralph loop script & agent instructions
status: done
category: enhancement
priority: 4
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

Rewrite `scripts/ralph.sh` and `CLAUDE.md` to use `.scratch/` markdown issues as the source of truth instead of `prd.json`. The loop mechanic stays the same — one issue per iteration, commit on pass — but reads and writes the new format.

## Acceptance criteria

- [ ] `scripts/ralph.sh` accepts `<feature-name>` as first argument and optional `--tool claude` flag (claude is default)
- [ ] Script reads issues from `.scratch/<feature-name>/issues/` where frontmatter `status: ready-for-agent`
- [ ] Script picks the lowest-numbered file (filename order enforces dependency order)
- [ ] Script exits cleanly when no `ready-for-agent` issues remain
- [ ] On successful iteration: agent sets `status: done` in the issue frontmatter
- [ ] Agent appends learnings to `.scratch/<feature-name>/progress.txt` (feature-scoped)
- [ ] Agent appends cross-cutting codebase patterns to `progress.txt` at project root (global)
- [ ] `CLAUDE.md` rewritten with updated instructions matching the new `.scratch/` format
- [ ] Stop condition: output contains `<promise>COMPLETE</promise>` OR no more `ready-for-agent` issues
- [ ] Old `prd.json`-based logic removed from `ralph.sh` and `CLAUDE.md`

## Blocked by

- BM-02

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

Core loop rewrite. The logic is identical to existing `ralph.sh` — only the data source changes from `prd.json` to `.scratch/` markdown files.

**Agent brief:**
- Read `.scratch/bm-kit/PRD.md`, existing `ralph.sh`, and existing `CLAUDE.md` for context
- Rewrite `scripts/ralph.sh`:
  - Arg 1: `<feature-name>`, optional `--tool claude` (default: claude)
  - Find issues: `grep -rl 'status: ready-for-agent' .scratch/<feature>/issues/` sorted by filename
  - Pick lowest-numbered file, pass to agent iteration
  - After iteration: update `status: ready-for-agent` → `status: done` in frontmatter
  - Append to `.scratch/<feature>/progress.txt` and root `progress.txt`
  - Exit when no `ready-for-agent` issues remain or `<promise>COMPLETE</promise>` detected
- Rewrite `CLAUDE.md` agent instructions to: read current issue from `.scratch/`, implement it, update status to done, append progress, commit
- Remove all `prd.json` references from both files
- Commit with `feat: BM-04 - Ralph loop script & agent instructions`
