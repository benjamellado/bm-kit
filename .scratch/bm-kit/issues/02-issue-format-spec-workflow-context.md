---
id: BM-02
title: Issue format spec & shared workflow context
status: done
category: enhancement
priority: 2
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

Define the canonical `.scratch/` markdown issue format that all skills read and write, and create the shared workflow context file that every skill loads. These are the two contracts the entire system depends on.

## Acceptance criteria

- [ ] `docs/agents/issue-tracker.md` updated to define the `.scratch/` format: directory structure, frontmatter fields (`id`, `title`, `status`, `priority`), valid status values (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`, `done`), and filename convention (`NN-slug.md`)
- [ ] `context/workflow.md` created at plugin root describing the full bm-kit pipeline: install → init → grill-me → grill-with-docs → to-prd → to-issues → triage → ralph
- [ ] `context/workflow.md` includes the purpose of each skill and what it produces
- [ ] `context/workflow.md` includes the `.scratch/` directory structure and status lifecycle

## Blocked by

- BM-01

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

Pure documentation work — no code changes. All decisions are captured in the PRD and grilling session.

**Agent brief:**
- Read `.scratch/bm-kit/PRD.md` for full context
- Update `docs/agents/issue-tracker.md` with the `.scratch/` format spec: directory layout, frontmatter fields (`id`, `title`, `status`, `priority`), valid status values, filename convention `NN-slug.md`
- Create `context/workflow.md` at plugin root covering: install → init → grill-me → grill-with-docs → to-prd → to-issues → triage → ralph, purpose of each skill, `.scratch/` structure, status lifecycle
- Commit with `feat: BM-02 - Issue format spec & shared workflow context`
