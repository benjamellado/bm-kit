---
id: BM-01
title: Repo foundation & restructure
status: done
category: enhancement
priority: 1
---

## Parent

`.scratch/bm-kit/PRD.md`

## What to build

Rename the plugin from `ralph-skills` to `bm-kit`, restructure the repository layout to match the new plugin design, migrate the git remote to the new repo, and remove obsolete artifacts. This is the foundation all other slices build on.

## Acceptance criteria

- [ ] `plugin.json` updated with `name: "bm-kit"` and skills path pointing to top-level `skills/`
- [ ] Top-level `skills/` directory created (replaces `.agents/skills/` as the active skills location)
- [ ] `scripts/` directory created with `ralph.sh` moved inside it
- [ ] Obsolete `skills/prd/` and `skills/ralph/` directories removed
- [ ] Git remote `origin` updated to `https://github.com/benjamellado/bm-kit.git`
- [ ] Fork relationship removed (repo is standalone)
- [ ] Existing skills from `.agents/skills/` copied into top-level `skills/` as starting point for rewrites

## Blocked by

None — can start immediately.

## Comments

> *This was generated by AI during triage.*

**Triage decision:** `enhancement` → `ready-for-agent`

All acceptance criteria are concrete and mechanical. One flag: changing the git remote is irreversible — the agent should confirm the target URL (`https://github.com/benjamellado/bm-kit.git`) with the user before executing `git remote set-url`.

**Agent brief:**
- Read `.scratch/bm-kit/PRD.md` for full context
- Start by updating `.claude-plugin/plugin.json`: set `name` to `bm-kit`, update `skills` path to `./skills/`
- Create `skills/` at repo root, copy contents of `.agents/skills/` into it
- Create `scripts/` at repo root, move `ralph.sh` into it
- Delete `skills/prd/` and `skills/ralph/` from the old location
- Before changing the git remote, confirm with user: "About to run `git remote set-url origin https://github.com/benjamellado/bm-kit.git` — proceed?"
- Commit all structural changes with `feat: BM-01 - Repo foundation & restructure`
